# 🧭 GraphRAG 민법 지식 그래프 — 15분 강의 기획 및 연동 명세 (LECTURE.MD)

본 문서는 **대한민국 민법 GraphRAG 프로젝트**의 구현 원리를 기반으로 한 15분 기술 강의 발표 가이드입니다. **데모 3 (실시간 RAG 쿼리) 시연 시간을 실제 구동 속도에 맞춰 컴팩트한 30초로 설정**하고, 발표 마지막 1분 동안 **"MS GraphRAG 설계 대조(Slide 09)"**와 **"결론 및 아키텍처의 미래 비전(Slide 10)"**을 단독 슬라이드로 쪼개어 리드미컬하고 여유 있게 클로징을 맺도록 다듬은 프레젠테이션 시나리오입니다.

---

## ⏱️ 1. 전체 타임라인 & 연동 매핑 (Total 15:00)

| 시간 (Time) | 세션 / 슬라이드 | 방식 | 3D Graph State | 발표 시연 흐름 (Lecture Flow) |
| :--- | :--- | :--- | :---: | :--- |
| **00:00 ~ 01:00** | **① GraphRAG 개요**<br>(슬라이드 1: 민법 조문 준용 체인과 그래프 모델링) | 슬라이드 | `STATE_IDLE` | 강의 인트로 및 조문 의존성 설명 |
| **01:00 ~ 02:30** | **🖥️ 데모 1: DB 전체 구조 (선공개)** | **라이브** | `STATE_GALAXY_VIEW` | **5,000 파티클 5대 편 은하 클러스터 시연** 🌟 |
| **02:30 ~ 04:00** | **② 데이터 적재 파이프라인 (1)**<br>(슬라이드 2: 정규식 개념 & 파싱 1, 2단계) | 슬라이드 | `STATE_GALAXY_VIEW` | `civil_act_parser.py` 위계 파싱 & 의미 청킹 |
| **04:00 ~ 05:30** | **② 데이터 적재 파이프라인 (2)**<br>(슬라이드 3: 왜 정규식인가? & 관계 추출 3단계) | 슬라이드 | `STATE_GALAXY_VIEW` | `hybrid_extractor.py` 준용/예외/참조 엣지 추출 |
| **05:30 ~ 07:00** | **② 데이터 적재 파이프라인 (3)**<br>(슬라이드 4: Neo4j 멱등 배치 적재 4단계) | 슬라이드 | `STATE_GALAXY_VIEW` | `neo4j_loader.py` UNIQUE CONSTRAINT & UNWIND 적재 |
| **07:00 ~ 08:30** | **③ 그래프 스키마 & 쿼리 엔진 (1)**<br>(슬라이드 5: Neo4j 노드/엣지 스키마 설계) | 슬라이드 | `STATE_GALAXY_VIEW` | 노드 프로퍼티 및 엣지 속성 구조 명세 |
| **08:30 ~ 10:00** | **③ 그래프 스키마 & 쿼리 엔진 (2)**<br>(슬라이드 6: 2-Hop Cypher 탐색의 의의) | 슬라이드 | `STATE_GALAXY_VIEW` | 실시간 다중 홉 탐색 및 최적 홉(Hop) 범위 분석 |
| **10:00 ~ 10:30** | **🖥️ 데모 2: 조문 탐색 시각화** | **라이브** | `STATE_GRAPH_TRAVERSAL` | **제13조 준용/예외 3D 레이저 빔 트래버설 시연** 🌟 |
| **10:30 ~ 12:00** | **④ RAG 추론 파이프라인 (1)**<br>(슬라이드 7: 4단계 실시간 추론 흐름) | 슬라이드 | `STATE_GRAPH_TRAVERSAL` | 질문 ➔ 매핑 ➔ 2-Hop ➔ Context ➔ LLM 구조 |
| **12:00 ~ 13:30** | **④ RAG 추론 파이프라인 (2)**<br>(슬라이드 8: Context Injection & 프롬프트 설계) | 슬라이드 | `STATE_GRAPH_TRAVERSAL` | LLM 컨텍스트 주입 방식 및 System Prompt 제약 |
| **13:30 ~ 14:00** | **🖥️ 데모 3: 실시간 RAG 쿼리 시연** | **라이브** | `STATE_GRAPH_TRAVERSAL` | **실시간 RAG 질의 ➔ 3D 레이저 및 AI 법률 보고서 데모** 🌟 |
| **14:00 ~ 14:30** | **⑤ 차별점 정리: 패러다임의 혁신**<br>(슬라이드 9: MS GraphRAG 비교 분석) | 슬라이드 | `STATE_BENCHMARK_RADAR` | 엔지니어링 패러다임 비교 분석 및 시사점 |
| **14:30 ~ 15:00** | **⑥ 결론 및 아키텍처 비전**<br>(슬라이드 10: 결정론과 확률론의 결합 & Q&A) | 슬라이드 | `STATE_GALAXY_VIEW` | 구조적 지식과 LLM의 조화 및 클로징 |

---

## 🏛️ 2. 슬라이드 & 라이브 데모 상세 명세 (강의 자료 포함)

---

### ① GraphRAG 개요 (00:00 ~ 01:00) — 슬라이드

#### 🎙️ 발표 핵심 메시지
* **민법 지식 그래프의 필요성**: 법률 조문은 독립된 정보 조각이 아닌 상호 연쇄적인 의존망을 형성합니다.
* **준용 체인의 의의**: 민법 제13조는 단독으로 완성되지 않으며, 준용 규정인 제14조와 예외 규정인 제16조를 함께 해석해야 온전한 법률 검토가 가능합니다. 문서를 단순 텍스트 조각이 아닌 **"노드(법조문) + 엣지(법률 관계)"**로 모델링하는 지식 그래프 RAG만이 법률 정보의 완전성을 보장합니다.

#### 🎞️ 슬라이드 1 — "민법 준용 체인과 그래프 모델링"
* **중앙 다이어그램**: 제13조를 기점으로 한 미니 온톨로지 모델링 도식.
  ```
  [제13조 피한정후견인의 행위] ──(준용)──▶ [제14조 종료심판]
              │
           (예외)
              ▼
  [제16조 피특정후견인의 행위]
  ```
* **강조 문구**: **"법률 정보는 텍스트가 아닌, 관계망의 형태로 복원되어야 한다"**

---

### 🖥️ 데모 1: 전체 DB 구조 (01:00 ~ 02:30) — 라이브

* **3D 그래프 반응**: 천천히 회전하던 3D 구체(`STATE_IDLE`)가 **정사면체 궤도를 가진 5대 편 은하 클러스터(`STATE_GALAXY_VIEW`)로 다이내믹하게 모핑**되며, 530개 온톨로지 엣지가 순차 점등됩니다.
* **강사 멘트**:
  > *"지금 화면에 렌더링된 3D 은하가 대한민국 민법 1,118개 조문 전체와 530개의 준용·예외·참조 온톨로지 관계를 완벽히 구축한 지식 그래프의 전경입니다. 중심에는 채권법 핵심 코어가 위치해 있고 외곽으로 총칙, 물권, 친족, 상속이 정사면체 궤도를 돌고 있습니다. 이 거대하고 정밀한 지식 그래프를 ₩0원으로 어떻게 자동 구축했는지 데이터 파이프라인 단계를 살펴보겠습니다."*

---

### ② 데이터 적재 파이프라인 4단계 (02:30 ~ 07:00) — 슬라이드

#### 🎙️ 발표 핵심 메시지 (ingest.py)
* **정규식(Regex)의 정의**: 특정한 규칙을 가진 문자열의 패턴을 탐지하고 정밀하게 추출하는 돋보기 도구.
* **결정론적 DB 빌드**: 비싸고 환각 가능성이 있는 LLM을 배제하고, 고도로 정형화된 법률 문법 패턴을 정규식으로 처리하여 오류율 0%(Zero-Loss)의 지식망 구축.

#### 🎞️ 슬라이드 2 — "정규식(Regex) 개념 & Pipeline 1, 2단계" (02:30 ~ 04:00)
* **시각 자료**: 
  * "정규식이란? 수만 줄의 글자 덩어리에서 `제\s*(\d+(?:의\d+)?)\s*조` 처럼 **규칙성(Pattern)만 골라 집어내는 스마트 돋보기**"
  * 민법 원문(.txt) ➔ 계층 파서(`civil_act_parser.py`) ➔ `편-장-절-조-항` 계층 트리 구조화 도식.
* **강사 큐카드**: 개정 가지번호(제14조의2)를 방어하는 정규식 구조와 동그라미 숫자(①) 기준의 '항(Clause)' 단위 세부 청킹 기법 설명.

#### 🎞️ 슬라이드 3 — "왜 LLM 대신 정규식인가? & Pipeline 3단계 관계 추출" (04:00 ~ 05:30)
* **시각 자료**: 
  * **LLM (확률론적 모델)**: 확률 예측 ➔ 조문 번호 환각 또는 관계 누락 위험. 법적 오류로 직결됨.
  * **정규식 (결정론적 모델)**: 약속된 규칙에 따라 작동 ➔ 오차율 0% (Zero-Loss). 민법 조문은 문법이 고도로 정형화되어 있어 정규식에 완벽히 최적화됨.
  * **비용/속도**: API 비용 ₩0, 로컬 CPU 연산으로 단 3초 만에 빌드 완료.
  * `"준용한다"`(🟢), `"불구하고"`/`"다만"`(🔴), 조문 인용(🔵) 키워드를 탐지하는 `hybrid_extractor.py` 관계 추출 메커니즘.
* **강사 큐카드**: 상대 참조("전조")의 이전 인덱스 해석과 범위("내지")의 개별 엣지 전개, 조건부 용어 변경("X는 Y로 본다")의 modifications 속성 추출 방법 설명.

#### 🎞️ 슬라이드 4 — "Pipeline 4단계(Neo4j 멱등 배치 적재)" (05:30 ~ 07:00)
* **시각 자료**: UNIQUE CONSTRAINT DDL 우선 적용 과정 및 500개 단위 `UNWIND` 배치 적재 파이프라인.
* **수치 강조**: `총 1,118개 조문 노드, 1,034개 항 서브노드, 530개 온톨로지 엣지 적재 / LLM 비용 ₩0`
* **강사 큐카드**: 데이터의 정합성을 보장하고 중복 유입을 차단하는 Neo4j MERGE 적재 원리 설명.

---

### ③ 그래프 스키마 & 쿼리 엔진 (07:00 ~ 10:00) — 슬라이드

#### 🎞️ 슬라이드 5 — "Neo4j 그래프 스키마 설계" (07:00 ~ 08:30)
* **시각 자료**: 조문(Article) 노드 프로퍼티 속성(id, base_number, title, fullText, contextPath) 테이블 및 엣지 속성 정의도.
* **강사 큐카드**: 노드 내부에 원문 텍스트 전체를 포함하여 런타임 검색 시 별도의 문서 조회 없이 컨텍스트를 즉시 구성하도록 최적화한 스키마 설계 설명.

#### 🎞️ 슬라이드 6 — "2-Hop Cypher 탐색의 의의" (08:30 ~ 10:00)
* **좌측**: `OPTIONAL MATCH path = (center)-[r*1..2]-(neighbor)` Cypher 쿼리 코드 박스.
* **우측**: 2-Hop 탐색 범위가 임계 수치(1-Hop 정보 부족 ↔ 3-Hop 노이즈 폭발) 상에서 왜 최적의 법률 RAG 스위트 스팟인지 설명하는 임계치 그래프.
* **강사 큐카드**: 질문의 진입 조문을 기점으로 법적 의존 조항 전체를 실시간 트래버설하여 dynamic subgraph를 형성하는 원리 설명.

---

### 🖥️ 데모 2: 조문 탐색 시각화 (10:00 ~ 10:30) — 라이브

* **3D 그래프 반응**: 카메라가 제1편 총칙 방향으로 줌인 포커싱하며, 제13조를 중심으로 **제14·15조로 녹색 빔(준용)이, 제16조로 빨간색 빔(예외)이 실시간 발사**됩니다. (30초간 노출)
* **강사 멘트**:
  > *"제13조를 호출해 실시간 2-Hop 탐색을 기동했습니다. 보시는 바와 같이 제13조와 연계된 제14조, 제15조, 제16조의 준용 및 예외 관계선이 3D 레이저 광선으로 실시간 투사됩니다. 지식 그래프만이 복원할 수 있는 관계의 경로입니다."*

---

### ④ RAG 추론 파이프라인 (10:30 ~ 13:30) — 슬라이드

#### 🎞️ 슬라이드 7 — "4단계 RAG 추론 파이프라인" (10:30 ~ 12:00)
* **시각 자료**: `질문 입력 ➔ 키워드 분석 ➔ Neo4j 2-Hop Traversal ➔ 컨텍스트 조립 ➔ Gemini API ➔ 보고서 생성` 4단계 플로우차트.
* **강사 큐카드**: 자연어 질의에서 핵심 법률 키워드를 도출하고, Neo4j 서브그래프를 추출하여 프롬프트의 컨텍스트(Context)로 엮는 흐름 설명.

#### 🎞️ 슬라이드 8 — "Context Injection & 프롬프트 설계" (12:00 ~ 13:30)
* **시각 자료**: RAG 프롬프트 템플릿(Context + System + Query) 레이아웃 및 system_prompt의 조문 인용 규칙 및 500자 제약 조건 가이드 박스.
* **강사 큐카드**: 지식 탐색(Neo4j)과 자연어 생성(Gemini)을 분리하여 환각을 철저히 통제하고, 제공된 온톨로지 컨텍스트 정보에만 의존하여 답변을 합성하도록 유도하는 프롬프트 엔지니어링 명세 설명.

---

### 🖥️ 데모 3: 실시간 RAG 쿼리 시연 (13:30 ~ 14:00) — 라이브

* **3D 그래프 반응**: `STATE_QUERYING` 상태의 스캔 애니메이션 후, **물권(제2편)과 채권(제3편) 클러스터가 동시에 밝아지며 다중 군집 프레이밍**이 일어납니다. 중심 제214조 ➔ 741조, 750조, 245조 노드들로 레이저가 가동되고 우측에 **AI 법률 보고서 패널이 오픈**됩니다. (30초 동안 심플하게 노출)
* **강사 멘트**:
  > *"질문을 인식하고 실시간 2-Hop 탐색이 이루어집니다. 3D 공간 상에서 물권(소유권)과 채권(부당이득/손배) 군집이 동시에 하이라이트되며 레이저로 연결됩니다. 우측 분석 보고서를 보시면 제214조, 제741조, 제750조를 정확하게 융합한 솔루션을 제시하고 있습니다."*

---

### ⑤ 차별점 및 결론 (14:30 ~ 15:00) — 슬라이드

#### 🎞️ 슬라이드 9 — "MS GraphRAG 비교 분석" (14:00 ~ 14:30)
* **시각 자료**: 두 GraphRAG 엔진의 기술 사양 비교 대조 테이블.
  * 관계 추출: MS(LLM 다량 호출 ➔ 고비용) vs 본 시스템(정규식 패턴 매칭 ➔ ₩0원)
  * RAG 컨텍스트: MS(커뮤니티 요약문 ➔ 추상적 추론) vs 본 시스템(조문 원문 ➔ 정밀한 100% 팩트 추론)
  * 탐색 범위: MS(전역/지역 요약 중심) vs 본 시스템(2-Hop 정밀 홉 탐색)
* **강사 멘트**:
  * *"마이크로소프트의 GraphRAG는 LLM을 사용해 비정형 엔티티를 범용적으로 묶지만, 수천 조항의 법률 도메인은 이미 정밀한 규칙으로 엣지가 정의되어 있습니다. 따라서 정규식 기반 결정론적 구축이 비용과 정확성 모든 면에서 압도적인 혁신을 만들어냅니다."*

#### 🎞️ 슬라이드 10 — "결론 및 아키텍처 비전" (14:30 ~ 15:00)
* **시각 자료**: 전체 파이프라인(Parser ➔ Extractor ➔ Neo4j ➔ Cypher ➔ LLM)을 도식화한 종합 요약도 및 Q&A 문구.
* **3D 그래프 반응**: 5,000 파티클이 전체 은하 뷰(`STATE_GALAXY_VIEW`)로 돌아가 평화롭게 회전.
* **강사 멘트**:
  * *"지식 그래프라는 단단한 규칙의 토대 위에 LLM의 유연한 자연어 문장 생성력이 결합할 때, 전문 지식 AI는 비로소 상용화 가능한 신뢰성을 갖추게 됩니다. 이것이 본 프로젝트가 입증한 하이브리드 RAG 아키텍처의 미래입니다. 경청해 주셔서 감사합니다."*

---

## 🎨 3. Active Theory 스타일의 슬라이드 HUD 디자인 스펙

3D 지식 그래프 우주 전체화면을 배경으로 삼고, 그 위에 반투명하게 떠 있는 아크릴 성질의 플로팅 카드를 오버레이하여 **시각적 압도감**과 **100% 팩트의 텍스트 가독성(가시성)**을 동시에 달성합니다.

### 🎛️ 1. 글래스모피즘 아크릴 카드 (Glassmorphism Card)
* **배경 투명도**: `background: rgba(10, 12, 16, 0.7)` (반투명 다크 그레이)
* **블러 효과**: `backdrop-filter: blur(18px) saturate(180%)`
* **보더 그라데이션**: 얇고 날카로운 경계선 처리 `border: 1px solid rgba(255, 255, 255, 0.08)`
* **그림자 글로우**: `box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37)` 및 슬라이드 상태별 아이덴티티 컬러(녹/적/청) 은은한 네온 글로우 처리.

### 🌫️ 2. 비하인드 블러 디밍 (Behind-Card Blur Dimming)
* **목적**: 3D 파티클(점)들이 회전하면서 텍스트 카드 뒷배경을 지나갈 때, 글자와 입자가 시각적으로 엉켜 가독성이 손실되는 현상을 완벽히 방지합니다.
* **구현**: 슬라이드 카드 오버레이 영역 바로 밑의 CSS 레이어에 `backdrop-filter: blur(10px)` 마스크를 동적으로 주어, 카드가 덮인 영역 뒷배경의 Three.js 캔버스만 흐릿하게 뭉개지도록 처리합니다.

### 📐 3. 반응형 레이아웃 및 여백 (Layout Grid)
* **배치**: 슬라이드 카드는 화면 좌측 35% 영역(`width: 380px` 내외)을 차지하며 고정 플로팅됩니다.
* **애니메이션**: 슬라이드가 전환될 때 이전 카드는 화면 좌측 바깥으로 부드럽게 미끄러지듯 퇴장(`x: -100%`)하고, 다음 카드가 페이드인되며 진입합니다 (`framer-motion` 이징: `[0.16, 1, 0.3, 1]`).

### ✍️ 4. 가시성 극대화 타이포그래피 (Typography)
* **글꼴**: 현대적이고 기하학적인 고대비 폰트 탑재 (Inter, system-ui).
* **색상 대비**: 주요 타이틀 및 수치 `color: #ffffff` (화이트), 본문 상세 설명 `color: #94a3b8` (뮤티드 그레이), 코드 및 강조 키워드는 온톨로지 에셋 색상(`color: #10b981`, `#38bdf8`, `#ef4444`) 적용.
* **행간 비율**: `line-height: 1.6` 및 `letter-spacing: -0.01em`를 고수하여, 프로젝터 대화면에서도 청중의 눈 피로도를 최소화하고 즉각적인 정보 스캔이 가능하도록 설계합니다.

### 🔄 5. 이질감 없는 심리스(Seamless) 시연 전환 메커니즘
이론 설명(슬라이드 카드 노출)과 실시간 데모 시연 간의 급격한 전환(Hard Cut)을 피하고 물 흐르듯 자연스럽게 화면을 전환하기 위한 기술적 구현 조건입니다.

1. **슬라이드 카드의 스프링 퇴장 & 입장**:
   * 음성 명령을 수신하는 즉시 슬라이드 카드는 화면 좌측 바깥(`x: -100%`, `opacity: 0`)으로 미끄러지듯 0.6초 이내에 페이드아웃 퇴장합니다. 시연이 종료되면 역순으로 화면 안으로 밀려 들어옵니다.
2. **3D 카메라의 동기화된 Lerp 이동**:
   * 슬라이드 카드의 퇴장/입장 타이밍에 맞춰 Three.js `useFrame` 루프 내부에서 카메라 위치(`camera.position`)와 타겟 지점(`lookAt`)을 부드러운 감쇠 곡선(Lerp)으로 선형 보간하여 날아가듯이 이동(Fly-through)시킵니다.
3. **에너지 펄스 기반 레이저 가동**:
   * 데모 2, 3의 레이저 광선 발사 시 단순한 선 그리기가 아닌, 시드 노드에서 타겟 노드로 광선이 전개되는 progressive 애니메이션을 구현하여 시선 흐름을 자연스럽게 유도합니다.
4. **AI 패널의 사이드-인 동기화**:
   * 실시간 RAG 결과 답변 시, 3D 레이저 애니메이션이 완성되는 타이밍(약 1.2초)에 맞추어 우측 AI 패널이 `framer-motion` 스프링 피직스를 타고 화면 우측에서 스무스하게 슬라이딩인되도록 트리거 딜레이를 동기화합니다.
